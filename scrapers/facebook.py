"""Facebook Pages placeholder scraper."""

from __future__ import annotations

from typing import Any


def scrape_facebook_pages(query: str, location: str = "", max_results: int = 100) -> list[dict[str, Any]]:
    # Facebook Graph API requires an app, token, permissions, and review in many cases.
    return []
