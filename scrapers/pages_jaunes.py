"""Pages Jaunes placeholder scraper.

This module is intentionally conservative. For production, use an official API or adapt it
only to sources whose terms allow automated collection.
"""

from __future__ import annotations

from typing import Any


def scrape_pages_jaunes(query: str, ville: str = "", pays: str = "", max_results: int = 100) -> list[dict[str, Any]]:
    # Placeholder to keep the app operational.
    # Replace with an allowed API/source implementation.
    return []
